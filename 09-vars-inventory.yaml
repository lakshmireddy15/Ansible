- name: variables from inventory
  hosts: local
  connection: local
  tasks:
  - name: print the course info
    ansible.buitin.debug:
      msg: " hello welcome to {{ COURSE }} with the help of {{ TRAINER }} time is {{DURATION}}"